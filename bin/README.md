You should set the environment variable PATH at system scope

```cmd
SETX /M PATH "%PATH%;E:\GitHub\blog\bin"
```

The the location in `post.cmd` should be in accord with the environment variable `PATH`.